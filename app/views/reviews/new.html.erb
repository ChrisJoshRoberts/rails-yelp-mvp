<div class="container">
  <h1>Review for: <strong><%= @restaurant.name %></strong> </h1>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :rating, :collection => %w[0 1 2 3 4 5] %>
    <%= f.input :content %>
    <%= f.submit class: "btn btn-primary"%>
  <% end %>
</div>
